<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.springboot.mybatis.MybatisMapper">

    <select id="getListEntity" resultType="cn.com.springboot.mybatis.MybatisEntity">
        SELECT
            s.student_code         AS studentCode,
            s.name                 AS name,
            s.age                  AS age,
            s.sex                  AS sex,
            a.sports               AS sports,
            a.student_organization AS studentOrganization,
            a.course               AS course
        FROM student s LEFT JOIN activity a ON s.student_code = a.activity_student_code
        limit #{limit,jdbcType=INTEGER}
        offset #{offset,jdbcType=INTEGER}
    </select>

</mapper>